import React, { ScrollView } from "react-native";

import { Footer } from "./components/Footer";

export const Weapons = () => {
  return (
    <ScrollView>
      
      <Footer />
    </ScrollView>
  );
}